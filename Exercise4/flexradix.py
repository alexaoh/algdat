from counting_sort import counting_sort

def flexradix(A, k):
    # Need to use counting_sort on each letter (as an int) until all the "digits"
    # are sorted from least significant to most significant. 
    
    # I was not able to find a solution to this problem now, but try again some other time!
